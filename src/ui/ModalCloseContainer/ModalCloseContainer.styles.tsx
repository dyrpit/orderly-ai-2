import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

export const ModalCloseContainer = styled(Box)(() => ({
 width: "60px",
 height: "60px",
 "& img": {
  cursor: "pointer",
 },
}));

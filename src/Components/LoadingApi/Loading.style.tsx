import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

export const Loading = styled(Box)(({}) => ({
    position: "absolute",
    left: "60vw",
    display: "none",
}));
